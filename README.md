The **cielo** command
=====================

To get help:

```text
cielo -h
```

Usage:

```text
cielo ( <dir> or <file> ) +
   -c convert *.cielo to *.coffee files
   -j convert *.cielo to *.js files
   -s convert *.starbucks to *.svelte files
   -h help"
```
